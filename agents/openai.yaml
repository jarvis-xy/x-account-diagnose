interface:
  display_name: "X Account Diagnose"
  short_description: "Local X archive diagnosis, no upload"
  default_prompt: "Use $x-account-diagnose to read my local X account archive and Under the Hood export, then write a formatted report: whether recommendations are restricted, whether the profile bio matches the posts, and creation advice from X's open-source ranking rules."
